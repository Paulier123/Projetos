const nomes = ["José", "Hugo", "Ju", "Simone", "Paulier"];

 //for (let i = 0; i < nomes.length; i++) {
  // console.log(nomes[i])}

for (nome of nomes ) {
   console.log(nome);
 }

//for(nome in nomes) {
  //console.log(typeof(nome.toString()));}
